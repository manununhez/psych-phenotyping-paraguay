# Copiar a `.env.docker` si se necesita pasar variables al contenedor.
#
# Solo es obligatoria si vas a ejecutar extracción LLM o scripts que dependan
# explícitamente de claves externas.

GEMINI_API_KEY=
